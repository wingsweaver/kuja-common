package com.wingsweaver.kuja.common.utils.support;

/**
 * 是否可选（非必须）的接口定义。
 *
 * @author wingsweaver
 */
public interface Optional {
    /**
     * 检查本实例是否可选（非必须）。
     *
     * @return 是否可选（非必须）
     */
    boolean isOptional();
}
